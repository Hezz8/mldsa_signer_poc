# Documentation Scripts

- `build_docs.sh`: builds the LaTeX documentation package when a local TeX toolchain is available.

The documentation package remains authoritative even when the local environment does not currently have `latexmk` or `pdflatex` installed.
