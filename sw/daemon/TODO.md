# Daemon TODO

- Generate language bindings from `sw/proto/signing.proto`
- Implement request validation for a strict 64-byte digest input
- Add MMIO orchestration state machine for one in-flight request in the PoC
- Add health and readiness endpoints or equivalent telemetry hooks
- Add integration tests against a stub MMIO backend
