# Implementation Prompt Notes

Use this directory for focused implementation prompts that target one subsystem at a time, such as the gRPC daemon, AXI-Lite wrapper, MMIO layer, or verification harness. Prompts should cite the controlling documents before code changes are requested.
