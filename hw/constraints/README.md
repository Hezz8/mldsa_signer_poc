# Constraints Area

This directory is reserved for board- and platform-specific constraints such as clock definitions, pin assignments, and timing exceptions.

No generated vendor constraints are committed at this stage.

For the first STUB-mode board image, future platform integration is expected to add only the minimum constraints needed for:

- clock hookup
- reset hookup
- AXI interconnect attachment through the platform design
- board-specific pin or platform requirements